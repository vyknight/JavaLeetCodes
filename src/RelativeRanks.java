import java.util.Arrays;
import java.util.HashMap;

public class RelativeRanks {
    public String[] findRelativeRanks(int[] score) {
        // use a map to map the score to the original thingy
        int[] sortedScore = score.clone();
        // copy the og score array
        // sort the copy
        // using the map, assign the value to each score
        // create a new array using the values from the sorted score, but the order of the og score
        // return
    }
}
